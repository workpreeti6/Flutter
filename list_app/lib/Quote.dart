import 'package:flutter/material.dart';

class Quote {
  String? author;
  String? text;
  // Quote(String author, String text) {
  //   this.author = author;
  //   this.text = text;
  // }
  Quote({this.text, this.author}); // named parameter by

// Quote(author:'preeti',text:'Life is beautiful') // calling
}